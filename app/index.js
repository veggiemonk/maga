
// test if creds are there and if they are OK
// forward to login if it is NOT OK

export default {
    controller: () => {

    },
    view: () => {
        return (
            <div>
                <h1>Hello Maga: App</h1>
            </div>
        )
    }
}