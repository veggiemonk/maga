// test if creds are there and if they are OK
// forward to app if it is ok

export default {
  controller: () => {

  },
  view: () => {
    return (
      <div>
        <h1>Hello Maga: Login</h1>
      </div>
    )
  }
}