var Menu = {

};

Menu.controller = function controller (attrs, children) {
  var c = {}

  return c;
}


Menu.view = function view (ctrl, attrs, children) {
  return (
    <div> MENU </div>
  )
}

export default Menu
